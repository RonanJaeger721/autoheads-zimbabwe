import Image from'next/image';import Link from'next/link';import{ArrowRight}from'lucide-react';import{SiteShell}from'@/components/site-shell';import{VehicleTabs}from'@/components/vehicle-tabs';import{vehicles}from'@/lib/autoheads-data';export default async function Page({params}:{params:Promise<{make:string,vehicle:string}>}){const{make,vehicle}=await params;const v=vehicles.find(x=>x.makeId===make&&x.id===vehicle)??vehicles[0];const related=vehicles.filter(x=>x.makeId===v.makeId&&x.id!==v.id);return <SiteShell><main className="vehicle-page vehicle-page-v2"><section className="vehicle-hero"><Image src={'image'in v&&v.image?v.image:'/images/hilux-road.webp'} fill priority alt={`${v.make} ${v.name}`}/><div/><nav className="crumbs"><Link href="/">Home</Link><span>/</span><Link href="/cars">Cars</Link><span>/</span><Link href={`/make/${v.makeId}`}>{v.make}</Link><span>/</span><b>{v.name}</b></nav><div className="vehicle-ghost">{v.name}</div><span>{v.make} / VEHICLE GUIDE</span><h1>{v.name}</h1><p>{v.intro}</p></section><VehicleTabs makeId={v.makeId} vehicleId={v.id} intro={v.intro}/><section className="vehicle-depth"><div><small>LOOKING AFTER THIS CAR?</small><h2>Keep it running.</h2><p>Move from the guide directly into maintenance knowledge and parts discovery.</p></div><Link href={`/list-shops?q=${encodeURIComponent('Filters')}`}>Find parts <ArrowRight/></Link><Link href="/list-mechanics">Find a mechanic <ArrowRight/></Link></section><section className="models-section"><span>YOU MAY ALSO LIKE</span><div>{related.map(x=><Link key={x.id} href={`/vehicle/${x.makeId}/${x.id}`}><small>{x.make}</small><b>{x.name}</b><ArrowRight/></Link>)}</div></section></main></SiteShell>}
